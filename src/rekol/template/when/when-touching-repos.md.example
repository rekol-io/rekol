---
name: Before touching a repo
description: Local conventions to follow before reading, cloning, or editing a repo. Learn and record over time.
type: when
tags: [repos, git, symlink, local]
aliases: [repo lookup, check a repo, look at a repo, clone a repo]
see_also: []
created: 2026-04-27T10:00:00-04:00
updated: 2026-04-27T10:00:00-04:00
---

# Before touching a repo

> **Directive (assistant):** Record the user's repo conventions here as you learn
> them — where repos live on disk, whether to prefer a local clone over cloning
> fresh, any symlink layout, and any rules about editing (for example, where edits
> are or aren't allowed). Capture a convention only after the user states it or
> you confirm it with them; never guess a path or a rule.

This file is task-triggered: its guidance surfaces when the user is about to read,
clone, or edit a repo. Until you have a real convention to record, leave the body
empty below this directive — an empty `when/` file is correct, and any text you
write here is treated as a standing instruction.

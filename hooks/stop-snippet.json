{
  "_comment": "Merge into ~/.claude/settings.json hooks.Stop. Records the assistant-completion timestamp so the next turn can compute elapsed deltas. Soft-fail: writes nothing to stdout and always exits 0.",
  "hooks": {
    "Stop": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "\"@REKOL@\" _hook record-stop 2>/dev/null || true"
          }
        ]
      }
    ]
  }
}

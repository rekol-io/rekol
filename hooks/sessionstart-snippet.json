{
  "hooks": {
    "SessionStart": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "if [ -n \"$MEMORY_HOME\" ] && [ -f \"$MEMORY_HOME/MEMORY.md\" ]; then echo '[memory] $MEMORY_HOME loaded — consult MEMORY.md, when/*.md, topics/*.md, or run memory-search'; cat \"$MEMORY_HOME/MEMORY.md\"; else echo '[memory] $MEMORY_HOME not configured'; fi"
          }
        ]
      }
    ]
  }
}

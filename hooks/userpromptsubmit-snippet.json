{
  "_comment": "Merge into ~/.claude/settings.json hooks.UserPromptSubmit. First handler injects an <env-time> block (local/UTC time plus elapsed-since-last-user/assistant). Second injects a ONE-TIME capture nudge when context usage crosses the threshold (#122) \u2014 silent unless the opt-in statusline recorder (docs/compaction.md) is wired. Soft-fail: always exit 0 so they never block a prompt.",
  "hooks": {
    "UserPromptSubmit": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "\"@REKOL@\" _hook time-context"
          }
        ]
      },
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "\"@REKOL@\" _hook capture-nudge 2>/dev/null || true"
          }
        ]
      }
    ]
  }
}
